# Rainfall-Predictor
A machine learning project that predicts whether it will rain tomorrow in Melbourne using a Random Forest Classifier trained on historical weather data. The model preprocesses features, performs hyperparameter tuning via Grid Search, and evaluates performance with classification metrics and confusion matrix.
